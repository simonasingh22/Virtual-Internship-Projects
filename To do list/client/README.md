# Todo Client

## Setup

1. Copy `.env.example` to `.env` and set `VITE_API_URL` to backend URL.
2. Install dependencies: `npm install`
3. Start dev server: `npm run dev`

## Deployment
- Build with `npm run build` and deploy to Vercel/Netlify. Make sure `VITE_API_URL` points to deployed backend.
